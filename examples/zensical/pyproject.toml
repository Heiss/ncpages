[project]
name = "ncpages-zensical-recipe"
version = "0.1.0"
description = "Build dependencies for the Zensical recipe, baked into the builder image"
requires-python = ">=3.13"
dependencies = [
    "zensical>=0.0.54",
]

[tool.uv]
package = false
